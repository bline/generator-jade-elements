language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-jade-element' ]; then cd .. && eval "mv $currentfolder generator-jade-element" && cd generator-jade-element; fi
